# Task 1 Report: Scope + Three Contracts + Codegen

## What Was Built

### New Contracts
- `tools/codegen/contracts/mailbox-create.ts` — `mailbox.create` capability: JMAP Mailbox/set create, `mail:mailbox` scope, non-destructive, agent-grade description with refusal codes `mailbox_name_conflict` / `mailbox_name_invalid`, two examples.
- `tools/codegen/contracts/mailbox-update.ts` — `mailbox.update` capability: JMAP Mailbox/set update (rename), `mail:mailbox` scope, non-destructive, agent-grade description with refusal codes `mailbox_protected` / `mailbox_name_invalid` / `mailbox_name_conflict`, one example.
- `tools/codegen/contracts/mailbox-delete.ts` — `mailbox.delete` capability: compound delete (Email/set + Mailbox/set), `mail:mailbox` scope, isDestructive + dryRun preview, agent-grade description with all four refusal codes (`mailbox_has_children`, `mailbox_protected`, `mailbox_forbidden`, `trash_not_found`), two examples.

### Scope Registry
- `docs/capability-scopes.md` — Added `mail:mailbox` row to the Mail section. No code enum existed; scopes are free strings. The doc is the normative registry.

### Test
- `tools/codegen/__tests__/mailbox-contracts.test.ts` — Reads `dist/tools/mailbox-delete.json` (correct path: `mailbox-delete.json`, not `mailbox.delete.json` as the brief's placeholder showed — codegen uses `safeName()` which converts dots to dashes). Asserts the description contains "Trash" and all three required refusal codes.

### Updated
- `tools/codegen/src/__tests__/run.test.ts` — Updated hardcoded capability count from 18 → 21 with updated comment listing the three new contracts.

## Codegen Output
`pnpm codegen` produced 21 capabilities. New generated files (gitignored, regenerated by CI):
- `tools/codegen/dist/tools/mailbox-create.json`
- `tools/codegen/dist/tools/mailbox-update.json`
- `tools/codegen/dist/tools/mailbox-delete.json`
- `shell/src/generated/capabilities/mailbox-create.ts`
- `shell/src/generated/capabilities/mailbox-update.ts`
- `shell/src/generated/capabilities/mailbox-delete.ts`

## Test Results (TDD Evidence)

All tests run from `tools/codegen/`:

```
Test Files  12 passed (12)
Tests  174 passed (174)
```

Focused test: `__tests__/mailbox-contracts.test.ts` — 1 test, PASS.

Shell typecheck: `cd shell && pnpm exec tsc -b --noEmit` — clean, no errors.

## Files Changed (Committed)
- `docs/capability-scopes.md` (modified — new `mail:mailbox` row)
- `tools/codegen/contracts/mailbox-create.ts` (new)
- `tools/codegen/contracts/mailbox-update.ts` (new)
- `tools/codegen/contracts/mailbox-delete.ts` (new)
- `tools/codegen/__tests__/mailbox-contracts.test.ts` (new)
- `tools/codegen/src/__tests__/run.test.ts` (modified — count 18 → 21)

## Self-Review Findings

1. **Test path mismatch from brief**: The brief showed `../dist/tools/mailbox.delete.json` but the codegen `safeName()` function converts dots to dashes. Actual path is `mailbox-delete.json`. Fixed in the test. This is correct behavior and consistent with all other generated tool files.

2. **No central scope enum to update**: The brief's Step 1 said to find a scope enum/array in codegen src. No such structure exists — scopes are unvalidated strings. The normative registry is `docs/capability-scopes.md`. Added the new scope there.

3. **Destructive contracts require dryRun**: `mailbox.create` and `mailbox.update` are non-destructive (no `dryRun`). `mailbox.delete` is destructive with `dryRun.preview` as required by the `capability()` helper. Matches the brief exactly.

## Concerns

None. Shell typecheck passed clean — the generated hooks for `mailbox.create`, `mailbox.update`, `mailbox.delete` use only primitive types (`string`, `boolean`, `number`) and the existing `useWriteHook` / `useReadHook` runtime hooks. No missing runtime types were introduced.
