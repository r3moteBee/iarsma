# Skip Puppeteer's bundled Chromium download — pa11y-ci (devDep) ships
# Puppeteer transitively, and we drive it against a system Chromium
# installed via the OS package manager (apt on CI, brew/apt locally).
# See docs/a11y.md.
puppeteer-skip-download=true
PUPPETEER_SKIP_DOWNLOAD=true
