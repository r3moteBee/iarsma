// WIT contract for html-sanitizer.
//
// HTML sanitizer using ammonia. Pure function: receives raw HTML, returns sanitized HTML. No network, no filesystem.
//
// Real interface lands in F-3 / Phase 0 work item 4.
// This placeholder lets the workspace resolve.

package iarsma:html-sanitizer@0.0.0;

world placeholder {
    // export hello: func() -> string;
}
