{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the iarsma webview.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
